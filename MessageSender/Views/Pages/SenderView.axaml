<UserControl
    x:Class="MessageSender.Views.Pages.SenderView"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:actipro="http://schemas.actiprosoftware.com/avaloniaui"
    xmlns:avaloniaEdit="clr-namespace:AvaloniaEdit;assembly=AvaloniaEdit"
    xmlns:converters="using:MessageSender.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:intraction="using:MessageSender.Services.Interaction"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:MessageSender.Models;assembly=MessageSender"
    xmlns:vm="clr-namespace:MessageSender.ViewModels.Pages;assembly=MessageSender"
    d:DesignHeight="450"
    d:DesignWidth="1000"
    x:DataType="vm:SenderViewModel"
    mc:Ignorable="d">

    <UserControl.Styles>
        <Style Selector="GridSplitter">
            <Setter Property="Background" Value="{actipro:ThemeResource ButtonBackgroundBrushOutlinePressed}" />
        </Style>
        <Style Selector="TextBlock.SmallLabel">
            <Setter Property="FontSize" Value="12" />
        </Style>
    </UserControl.Styles>

    <Grid ColumnDefinitions="*,1,*">
        <Grid Grid.Column="0" RowDefinitions="Auto, Auto, 4*,1,2*">
            <Border
                Grid.Row="0"
                Height="43"
                Padding="5"
                Background="{actipro:ThemeResource Container2BackgroundBrush}"
                BorderBrush="{actipro:ThemeResource Container3BorderBrush}"
                BorderThickness="0,0,0,1">
                <Grid ColumnDefinitions="*, Auto">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <ComboBox
                            MinWidth="200"
                            Classes="theme-soft"
                            IsEnabled="{Binding !AppState.AppData.IsDeviceConnected}"
                            ItemsSource="{Binding AppState.AppData.Devices}"
                            PlaceholderText="Device Id"
                            SelectedIndex="{Binding AppState.AppData.SelectedDeviceIndex}"
                            SelectedItem="{Binding AppState.AppData.SelectedDevice}"
                            ToolTip.Tip="{Binding AppState.AppData.SelectedDevice.Description}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate DataType="{x:Type models:Device}">
                                    <Grid ColumnDefinitions="Auto, Auto, Auto" ToolTip.Tip="{Binding Description}">
                                        <TextBlock
                                            Grid.Column="0"
                                            MinWidth="130"
                                            FontWeight="DemiBold"
                                            Text="{Binding DeviceId}"
                                            TextAlignment="Left" />
                                        <TextBlock
                                            Grid.Column="1"
                                            MinWidth="130"
                                            Text="{Binding DeviceType}"
                                            TextAlignment="Center" />
                                        <TextBlock
                                            Grid.Column="2"
                                            Text="{Binding Environment}"
                                            TextAlignment="Right" />
                                    </Grid>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                        <Button
                            Name="ConnectBtn"
                            Classes="theme"
                            Command="{Binding ConnectCommand}"
                            Content="{Binding ConnectButtonText}" />
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <Button
                            Classes="theme-subtle accent"
                            Command="{Binding SendMessageCommand}"
                            IsVisible="{Binding AppState.AppData.IsDeviceConnected}"
                            ToolTip.Tip="Send message">
                            <PathIcon Data="{StaticResource SendRegular}" />
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
            <Border
                Grid.Row="1"
                Height="43"
                Padding="5"
                Background="{actipro:ThemeResource Container2BackgroundBrush}"
                BorderBrush="{actipro:ThemeResource Container3BorderBrush}"
                BorderThickness="0,0,0,1">
                <Grid ColumnDefinitions="*, Auto">
                    <ComboBox
                        Grid.Column="0"
                        MinWidth="220"
                        Classes="theme-soft"
                        ItemsSource="{Binding AppState.AppData.Messages}"
                        PlaceholderText="Load saved message..."
                        SelectedItem="{Binding SelectedStoredMessage}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate DataType="{x:Type models:StoredMessage}">
                                <Grid ColumnDefinitions="Auto, Auto">
                                    <TextBlock
                                        Grid.Column="0"
                                        MinWidth="160"
                                        FontWeight="DemiBold"
                                        Text="{Binding Name}" />
                                    <TextBlock
                                        Grid.Column="1"
                                        Foreground="{actipro:ThemeResource DefaultForegroundBrushDisabled}"
                                        Text="{Binding DeviceType}" />
                                </Grid>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <StackPanel
                        Grid.Column="1"
                        Orientation="Horizontal"
                        Spacing="2">
                        <Button
                            Classes="theme-subtle"
                            Command="{Binding SelectCdmDefinitionsPathCommand}"
                            ToolTip.Tip="{Binding AppState.Settings.CdmDefinitionsPath, StringFormat='CDM Definitions: {0}'}">
                            <PathIcon Data="{StaticResource FolderRegular}" />
                        </Button>
                        <!--  Warning: CDM definitions not loaded  -->
                        <Path
                            Width="20"
                            Height="20"
                            Margin="-4,-2,0,0"
                            VerticalAlignment="Center"
                            Data="{StaticResource WarningRegular}"
                            Fill="#DC3545"
                            IsVisible="{Binding ShouldShowCdmWarning}"
                            ToolTip.Tip="CDM definitions not loaded. Click the folder button to configure a path to a local copy of the cdm-definitions repository (https://github.com/resideo/cdm-definitions)." />
                    </StackPanel>
                </Grid>
            </Border>
            <Border Grid.Row="2">
                <Grid>
                    <Border
                        Height="32"
                        Margin="0,0,15,10"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Bottom"
                        Background="{actipro:ThemeResource Container2BackgroundBrush}"
                        BorderBrush="{actipro:ThemeResource Container2BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="5"
                        ZIndex="10">
                        <StackPanel Orientation="Horizontal">
                            <!--  Show when in CDM view mode: click to switch to raw edit  -->
                            <Button
                                Width="32"
                                Padding="0"
                                Classes="theme-subtle accent"
                                Command="{Binding ToggleCdmEditModeCommand}"
                                IsVisible="{Binding IsCdmViewMode}"
                                ToolTip.Tip="Edit raw JSON">
                                <PathIcon Data="{StaticResource EditRegular}" />
                            </Button>
                            <!--  Show when in CDM edit mode (raw): click to switch to CDM view  -->
                            <Button
                                Width="32"
                                Padding="0"
                                Classes="theme-subtle"
                                Command="{Binding ToggleCdmEditModeCommand}"
                                IsVisible="{Binding IsCdmEditActive}"
                                ToolTip.Tip="View CDM format">
                                <PathIcon Data="{StaticResource EyeRegular}" />
                            </Button>
                            <Button
                                Width="32"
                                Padding="0"
                                Classes="theme-subtle"
                                Command="{Binding BeautifyCommand}"
                                ToolTip.Tip="Beautify">
                                <TextBlock
                                    Margin="0,0,0,5"
                                    Padding="0"
                                    FontSize="18"
                                    FontWeight="SemiBold">
                                    { }
                                </TextBlock>
                            </Button>
                            <!--  Warning: CDM definitions not loaded  -->
                            <Path
                                Width="20"
                                Height="20"
                                Margin="-4,-2,0,0"
                                VerticalAlignment="Center"
                                Data="{StaticResource WarningRegular}"
                                Fill="#DC3545"
                                IsVisible="{Binding ShouldShowCdmWarning}"
                                ToolTip.Tip="CDM definitions not loaded. Click the folder button to configure a path to a local copy of the cdm-definitions repository (https://github.com/resideo/cdm-definitions)." />
                            <!--  Save changes to selected message (Ctrl+S)  -->
                            <Button
                                Width="32"
                                Padding="0"
                                Classes="theme-subtle"
                                Command="{Binding SaveSelectedMessageCommand}"
                                IsEnabled="{Binding SelectedStoredMessage, Converter={x:Static ObjectConverters.IsNotNull}}"
                                ToolTip.Tip="Save changes to selected message (Ctrl+S)">
                                <PathIcon Data="{StaticResource SaveRegular}" />
                            </Button>
                            <Button
                                Width="32"
                                Padding="0"
                                Classes="theme-subtle"
                                Command="{Binding SaveAsMessagesCommand}"
                                ToolTip.Tip="Save as new message">
                                <PathIcon Data="{StaticResource SaveAsRegular}" />
                            </Button>
                        </StackPanel>
                    </Border>
                    <avaloniaEdit:TextEditor
                        x:Name="MessageBodyEditor"
                        Margin="0"
                        Background="{actipro:ThemeResource Container1BackgroundBrush}"
                        Document="{Binding AppState.AppData.MessageBody}"
                        FontFamily="{StaticResource JetBrainsMono}"
                        IsVisible="{Binding IsCdmViewMode, Converter={x:Static BoolConverters.Not}}" />
                    <!--  Read-only CDM human-readable view  -->
                    <avaloniaEdit:TextEditor
                        x:Name="CdmBodyEditor"
                        Margin="0"
                        Background="{actipro:ThemeResource Container1BackgroundBrush}"
                        Document="{Binding CdmRenderedBody}"
                        FontFamily="{StaticResource JetBrainsMono}"
                        IsReadOnly="True"
                        IsVisible="{Binding IsCdmViewMode}" />
                </Grid>
            </Border>
            <GridSplitter
                Grid.Row="3"
                MinHeight="3"
                MaxHeight="3"
                ResizeDirection="Rows" />
            <Border Grid.Row="4">
                <Grid>
                    <avaloniaEdit:TextEditor
                        Name="MessageHeadersEditor"
                        Background="{actipro:ThemeResource Container1BackgroundBrush}"
                        Document="{Binding AppState.AppData.UserProperties}"
                        FontFamily="{StaticResource JetBrainsMono}"
                        IsVisible="{Binding IsCdmViewMode, Converter={x:Static BoolConverters.Not}}"
                        ShowLineNumbers="True" />
                    <!--  Read-only CDM rendered properties view  -->
                    <avaloniaEdit:TextEditor
                        x:Name="CdmPropertiesEditor"
                        Background="{actipro:ThemeResource Container1BackgroundBrush}"
                        Document="{Binding CdmRenderedProperties}"
                        FontFamily="{StaticResource JetBrainsMono}"
                        IsReadOnly="True"
                        IsVisible="{Binding IsCdmViewMode}"
                        ShowLineNumbers="True" />
                </Grid>
            </Border>
        </Grid>

        <GridSplitter
            Grid.Column="1"
            MinWidth="3"
            MaxWidth="3" />
        <Grid Grid.Column="2" RowDefinitions="Auto, *,1,*">
            <Border
                Grid.Row="0"
                Height="43"
                Padding="5"
                Background="{actipro:ThemeResource Container2BackgroundBrush}"
                BorderBrush="{actipro:ThemeResource Container3BorderBrush}"
                BorderThickness="0,0,0,1">
                <Grid ColumnDefinitions="*, Auto">
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <Button
                            Command="{Binding ClearIncomingMessagesCommand}"
                            Content="Clear"
                            ToolTip.Tip="Clear message list" />
                    </StackPanel>
                    <StackPanel
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        Orientation="Horizontal"
                        Spacing="5">
                        <TextBlock Text="QTY:" />
                        <TextBlock Classes="theme-text-heading size-xs accent" Text="{Binding AppState.AppData.DeviceMessages.Count}" />
                    </StackPanel>
                </Grid>
            </Border>
            <Border Grid.Row="1">
                <ListBox
                    BorderThickness="0"
                    ItemsSource="{Binding AppState.AppData.DeviceMessages}"
                    SelectedItem="{Binding SelectedMessage}">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ReversibleStackPanel ReverseOrder="True" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate x:DataType="intraction:DeviceMessage">
                            <Grid ColumnDefinitions="*, Auto">
                                <StackPanel
                                    Grid.Column="0"
                                    Orientation="Horizontal"
                                    Spacing="3">
                                    <TextBlock
                                        Width="33"
                                        Classes="theme-text-heading size-xs"
                                        Text="{Binding Direction, Converter={x:Static converters:MessageDirectionConverter.Instance}}" />
                                    <Border
                                        Padding="2,0"
                                        BorderBrush="#ccced3"
                                        BorderThickness="1"
                                        CornerRadius="3">
                                        <TextBlock Classes="theme-text-code size-sm" Text="{Binding Method}" />
                                    </Border>
                                    <Border
                                        Padding="2,0"
                                        Background="{Binding Source, Converter={x:Static converters:MessageSourceToColorConverter.Instance}}"
                                        BorderBrush="#ccced3"
                                        BorderThickness="1"
                                        CornerRadius="3">
                                        <TextBlock Classes="theme-text-code size-sm" Text="{Binding Source}" />
                                    </Border>
                                </StackPanel>
                                <TextBlock
                                    Grid.Column="1"
                                    HorizontalAlignment="Right"
                                    Text="{Binding Timestamp, StringFormat={}{0:d MMM yyyy @ HH:mm:ssss.fff}}" />
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
            <GridSplitter
                Grid.Row="2"
                MinHeight="3"
                MaxHeight="3" />
            <Border Grid.Row="3">
                <avaloniaEdit:TextEditor
                    Name="SelectedIncomingMessageEditor"
                    Background="{actipro:ThemeResource Container1BackgroundBrush}"
                    Document="{Binding IncomingMessage}"
                    FontFamily="{StaticResource JetBrainsMono}"
                    SearchResultsBrush="Red"
                    ShowLineNumbers="True" />
            </Border>
        </Grid>
    </Grid>
</UserControl>